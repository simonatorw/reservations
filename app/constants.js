export const SERVER = 'http://localhost:1628';

export const dataUrls = {
	ALL: `${SERVER}/reservations`,
	BY_ID: `${SERVER}/reservation/`,
	ADD_NEW: `${SERVER}/reservation`,
	QUERY: `${SERVER}/reservations?`
};